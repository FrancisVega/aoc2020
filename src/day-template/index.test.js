const { foo } = require("./index")

describe("Day x: ...", () => {
  describe("Part 1", () => {
    test("should...", () => {
      expect(foo(true)).toBe(true)
    })
  })
  describe("Part 2", () => {
    test("should...", () => {
      expect(foo(false)).toBe(false)
    })
  })
})
